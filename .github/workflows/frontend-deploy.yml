name: Deploy Frontend (Vite + React) to Azure

on:
  push:
    branches:
      - main
    paths:
      - 'classroom-booking/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        working-directory: classroom-booking
        run: npm ci

      - name: Build project (Vite)
        working-directory: classroom-booking
        run: npm run build

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: classroom-booking-frontend-d
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_FRONTEND }}
          package: classroom-booking/dist
